---
phase: 01-foundation-layout
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/sheet.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/separator.tsx
  - src/components/layout/MobileMenu.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/index.ts
autonomous: true

must_haves:
  truths:
    - "User can open mobile menu via hamburger icon on mobile"
    - "Mobile menu shows all navigation links"
    - "Mobile menu slides in from correct side (left LTR, right RTL)"
    - "Footer shows 3 office locations in tabs"
    - "Footer shows PO Box and quick links"
    - "Hamburger icon animates to X when menu open"
  artifacts:
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component for mobile drawer"
      contains: "SheetContent"
    - path: "src/components/ui/tabs.tsx"
      provides: "shadcn Tabs component for office locations"
      contains: "TabsContent"
    - path: "src/components/layout/MobileMenu.tsx"
      provides: "Mobile navigation drawer"
      min_lines: 40
      contains: "Sheet"
    - path: "src/components/layout/Footer.tsx"
      provides: "Full footer with offices, links, about"
      min_lines: 80
      contains: "Tabs"
  key_links:
    - from: "src/components/layout/MobileMenu.tsx"
      to: "translations"
      via: "useTranslation hook"
      pattern: "t\\('nav\\."
    - from: "src/components/layout/MobileMenu.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "Sheet component"
      pattern: "Sheet"
    - from: "src/components/layout/Footer.tsx"
      to: "src/components/ui/tabs.tsx"
      via: "Tabs component"
      pattern: "Tabs"
    - from: "src/components/layout/Header.tsx"
      to: "src/components/layout/MobileMenu.tsx"
      via: "hamburger trigger"
      pattern: "MobileMenu"
---

<objective>
Build mobile hamburger menu and full footer with office location tabs.

Purpose: Mobile users need navigation access via hamburger menu. Footer provides essential company info (offices, contact, quick links) that appears on every page.

Output: MobileMenu component with Sheet drawer and animated hamburger icon. Footer with tabs for 3 offices (Basrah, Erbil, Baghdad), quick links, about snippet, and social links.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-layout/01-CONTEXT.md
@.planning/phases/01-foundation-layout/01-RESEARCH.md
@SHADCN.md
@FRAMER-MOTION.md
@TAILWIND.md

@src/components/layout/Footer.tsx
@src/components/layout/Header.tsx
@src/stores/useUIStore.ts
@src/locales/en/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create MobileMenu</name>
  <files>
    - src/components/ui/sheet.tsx
    - src/components/ui/tabs.tsx
    - src/components/ui/separator.tsx
    - src/components/layout/MobileMenu.tsx
    - src/components/layout/index.ts
  </files>
  <action>
1. **Install shadcn components**:
```bash
pnpm dlx shadcn@latest add sheet tabs separator
```

2. **Create src/components/layout/MobileMenu.tsx**:
- Use Sheet from shadcn for the drawer
- Determine side based on language direction: `side={i18n.dir() === 'rtl' ? 'right' : 'left'}`
- Trigger button: hamburger icon that animates to X when open
  - Use motion.svg with 3 paths
  - Animate between hamburger lines and X shape
  - Button visible only on mobile: `className="md:hidden"`
- Sheet content width: `w-[280px] sm:w-[320px]`
- Inside Sheet:
  - Logo at top
  - Navigation links (same as desktop, but stacked vertically)
  - Language switcher at bottom
  - Each nav link closes the menu when clicked (setOpen(false))
- Navigation links:
  - Use NavLink from react-router
  - Full width, py-3 padding
  - Active state: text-primary, bg-accent
  - Same 6 links as Navigation component

3. **Hamburger animation** (simplified approach):
```tsx
// Use lucide icons with conditional render
import { Menu, X } from 'lucide-react'

// In button:
{open ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}

// Or for animated hamburger (optional enhancement):
// Use Framer Motion to morph between states
```

4. **Export from index.ts**:
- Add `export { MobileMenu } from './MobileMenu'`
  </action>
  <verify>
- shadcn components installed: sheet.tsx, tabs.tsx, separator.tsx exist in ui/
- `pnpm build` succeeds
- MobileMenu component exports correctly
  </verify>
  <done>
Sheet, Tabs, Separator shadcn components installed. MobileMenu created with hamburger trigger, RTL-aware side, and full navigation links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build full Footer with office tabs and update Header with mobile menu</name>
  <files>
    - src/components/layout/Footer.tsx
    - src/components/layout/Header.tsx
  </files>
  <action>
1. **Rebuild src/components/layout/Footer.tsx** (per CONTEXT.md: multiple sections with office tabs):

Structure (responsive grid):
```
Desktop (lg+): 4 columns
- Column 1: About snippet (company description)
- Column 2: Quick Links (nav links)
- Column 3: Office tabs (Basrah, Erbil, Baghdad)
- Column 4: Contact/Social

Mobile: Stack vertically
```

Implementation details:
- Use Container component for consistent width
- Background: `bg-muted/30` with `border-t` (subtle separator per CONTEXT.md: light footer)
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8`

**About Section:**
- Title: `t('footer.aboutTitle')`
- Text: `t('footer.aboutText')`
- Company name/logo

**Quick Links Section:**
- Title: `t('footer.quickLinks')`
- Same 6 nav links as header (stacked, with hover color)
- Use Link from react-router

**Offices Section (Tabs per CONTEXT.md decision):**
- Use Tabs component from shadcn
- Title: `t('footer.officesTitle')`
- TabsList with 3 triggers: Basrah, Erbil, Baghdad
- Each TabsContent shows:
  - Address (with MapPin icon)
  - Phone (with Phone icon, wrapped in `<a href="tel:..."`)
  - Email (with Mail icon, wrapped in `<a href="mailto:..."`)
- Tab triggers use translation: `t('footer.offices.basrah.name')` etc.

**Social/Contact Section:**
- Title: `t('footer.followUs')`
- LinkedIn icon (linked if URL known, placeholder otherwise)
- PO Box: `t('footer.poBox')`

**Copyright Row:**
- Below main grid
- Separator component
- Centered text: `{currentYear} {siteName}. {allRightsReserved}`

2. **Update src/components/layout/Header.tsx**:
- Import MobileMenu component
- Add MobileMenu to Actions area (before LanguageSwitcher)
- MobileMenu's trigger button is `md:hidden`, so it only shows on mobile
- Desktop nav is `hidden md:flex`, so they swap correctly

Icons to use (from lucide-react):
- MapPin for address
- Phone for phone
- Mail for email
- Linkedin for social
  </action>
  <verify>
- `pnpm dev` shows full footer with 4-column layout on desktop
- Footer collapses to stacked layout on mobile
- Office tabs work: clicking "Erbil" shows Erbil contact info
- Phone numbers are clickable (tel: links)
- Emails are clickable (mailto: links)
- Hamburger menu appears on mobile, desktop nav appears on desktop
- Mobile menu opens when hamburger clicked
- Mobile menu slides from left (LTR) or right (RTL)
- All footer text displays in selected language
  </verify>
  <done>
Footer displays about text, quick links, office tabs (3 offices), social links, PO Box, and copyright. Mobile menu hamburger appears on mobile and opens Sheet with navigation. RTL side switching works.
  </done>
</task>

</tasks>

<verification>
Phase 01-03 verification:
1. **Mobile menu**: Hamburger visible on mobile, opens Sheet drawer
2. **RTL menu side**: Menu slides from right in Arabic, left in English
3. **Mobile nav links**: All 6 links present, close menu on click
4. **Footer sections**: About, Quick Links, Offices, Social all render
5. **Office tabs**: Click tab to see different office contact info
6. **Clickable contacts**: Phone (tel:) and email (mailto:) links work
7. **Responsive layout**: 4 columns on lg, 2 on md, 1 on mobile
8. **Translation**: All footer text in correct language
</verification>

<success_criteria>
- [ ] shadcn Sheet, Tabs, Separator components installed
- [ ] Hamburger menu appears on mobile (md:hidden)
- [ ] Clicking hamburger opens Sheet with navigation
- [ ] Mobile menu side matches language direction (left LTR, right RTL)
- [ ] Mobile menu links close menu when clicked
- [ ] Footer has 4 sections: About, Quick Links, Offices, Social
- [ ] Offices section uses Tabs with 3 offices
- [ ] Each office shows address, phone (clickable), email (clickable)
- [ ] PO Box displayed in footer
- [ ] Copyright row with current year
- [ ] All footer content displays in selected language
- [ ] Responsive: 4-col desktop, stacked mobile
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-layout/01-03-SUMMARY.md`
</output>
