---
phase: 04-services-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/services.ts
  - src/locales/en/services.json
  - src/locales/ar/services.json
  - src/lib/i18n.ts
autonomous: true

must_haves:
  truths:
    - "All 26 services from company profile are defined with correct categorization"
    - "Each service has English title and description"
    - "Each service has Arabic title and description"
    - "Services namespace loads correctly in i18n"
  artifacts:
    - path: "src/data/services.ts"
      provides: "Service type definitions and data array"
      exports: ["Service", "ServiceCategory", "services"]
    - path: "src/locales/en/services.json"
      provides: "English translations for all services"
      contains: "items.coiledTubing.title"
    - path: "src/locales/ar/services.json"
      provides: "Arabic translations for all services"
      contains: "items.coiledTubing.title"
  key_links:
    - from: "src/data/services.ts"
      to: "src/locales/*/services.json"
      via: "service.id matches translation key"
      pattern: "items\\.\\w+\\.title"
---

<objective>
Create the service data structure and complete bilingual translations for all 26 services.

Purpose: Establish the data foundation for the Services page. This defines the service taxonomy, icon mappings, and all translatable content that the UI components will consume.

Output: Type-safe service data file and complete English/Arabic translation files ready for UI consumption.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/JERASH-PROFILE.md
@.planning/phases/04-services-page/04-RESEARCH.md
@.planning/phases/04-services-page/04-CONTEXT.md

# Existing patterns
@src/lib/i18n.ts
@src/locales/en/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service data structure</name>
  <files>src/data/services.ts</files>
  <action>
Create `src/data/services.ts` with:

1. Type definitions:
   - `ServiceCategory = 'production' | 'wireline' | 'consultancy' | 'other'`
   - `Service = { id: string; category: ServiceCategory; icon: LucideIcon }`

2. Services array with all 26 services from JERASH-PROFILE.md:

**Production (8):**
- coiledTubing (Cog)
- drillingFluid (Droplet)
- cementing (Layers)
- nitrogen (FlaskConical)
- filtration (Pipette)
- pipelines (Cable)
- waterProduction (Droplet)
- artificialLift (Target)

**Wireline (7):**
- wirelineLogging (Activity)
- wellTesting (TestTube)
- stimulationPumping (Zap)
- thruTubing (Wrench)
- slickline (Cable)
- completions (Settings)
- scaffolding (Boxes)

**Consultancy (8):**
- importMachinery (Truck)
- importChemicals (FlaskConical)
- importGasStation (Fuel)
- processing (Factory)
- customsClearance (ClipboardCheck)
- explosives (Flame)
- inspection (FileSearch)
- training (GraduationCap)

**Other (3):**
- fuelStations (Fuel)
- mudLogging (Gauge)
- integratedDrilling (Compass)

Export: `services`, `Service`, `ServiceCategory`
  </action>
  <verify>
    - `pnpm tsc --noEmit` passes with no type errors
    - File exports Service, ServiceCategory types and services array
    - services.length === 26
  </verify>
  <done>
    - All 26 services defined with correct categories
    - Each service has unique id matching translation keys
    - Icon imports from lucide-react
  </done>
</task>

<task type="auto">
  <name>Task 2: Create English service translations</name>
  <files>src/locales/en/services.json</files>
  <action>
Create `src/locales/en/services.json` with:

```json
{
  "title": "Our Services",
  "description": "Comprehensive oil field solutions for your needs",
  "categories": {
    "all": "All",
    "production": "Production",
    "wireline": "Wireline",
    "consultancy": "Consultancy",
    "other": "Other"
  },
  "items": {
    // For each of the 26 services:
    "serviceId": {
      "title": "Service Title",
      "shortDescription": "1-2 sentence preview for card",
      "description": "Full description from company profile"
    }
  }
}
```

Use the detailed service descriptions from JERASH-PROFILE.md for the `description` field.
Create concise `shortDescription` (under 100 chars) summarizing each service for card preview.

All 26 services must have entries with these 3 fields.
  </action>
  <verify>
    - Valid JSON (no syntax errors)
    - All 26 service IDs have title, shortDescription, description
    - Descriptions match company profile content
  </verify>
  <done>
    - Complete English translations for all services
    - Category labels translated
    - Page title and meta description included
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Arabic service translations and update i18n</name>
  <files>src/locales/ar/services.json, src/lib/i18n.ts</files>
  <action>
1. Create `src/locales/ar/services.json` with Arabic translations:

```json
{
  "title": "خدماتنا",
  "description": "حلول شاملة لحقول النفط لتلبية احتياجاتكم",
  "categories": {
    "all": "الكل",
    "production": "الإنتاج",
    "wireline": "السلكي",
    "consultancy": "الاستشارات",
    "other": "أخرى"
  },
  "items": {
    // Arabic translations for all 26 services
  }
}
```

For service translations, use professional Arabic oil industry terminology:
- Technical accuracy is important (these are real oil field services)
- If unsure of exact Arabic term, use transliterated English in parentheses

2. Update `src/lib/i18n.ts`:
- Add 'services' to the `ns` array: `ns: ['common', 'services']`

This ensures the services namespace is preloaded.
  </action>
  <verify>
    - Valid JSON (no syntax errors)
    - All 26 service IDs have title, shortDescription, description in Arabic
    - i18n ns array includes 'services'
    - `pnpm build` succeeds (translations loadable)
  </verify>
  <done>
    - Complete Arabic translations for all services
    - Services namespace registered in i18n
    - Translations can be loaded via useTranslation('services')
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Type check passes: `pnpm tsc --noEmit`
2. Build succeeds: `pnpm build`
3. Services data exports correctly:
   ```typescript
   import { services, type Service, type ServiceCategory } from '@/data/services'
   console.log(services.length) // 26
   ```
4. Translations loadable in both languages
</verification>

<success_criteria>
- 26 services defined in data file with typed categories and icons
- Complete English translations (title + shortDescription + description) for all services
- Complete Arabic translations for all services
- i18n configured to load services namespace
- No TypeScript errors, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-services-page/04-01-SUMMARY.md`
</output>
