---
phase: 06-gallery-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/data/gallery-images.ts
  - src/locales/en/gallery.json
  - src/locales/ar/gallery.json
autonomous: true

must_haves:
  truths:
    - "Gallery image metadata defines all 26 images with dimensions"
    - "Each image has descriptive alt text for accessibility"
    - "Gallery translations available in Arabic and English"
  artifacts:
    - path: "src/data/gallery-images.ts"
      provides: "GalleryImage type and galleryImages array"
      exports: ["GalleryImage", "galleryImages"]
      min_lines: 80
    - path: "src/locales/en/gallery.json"
      provides: "English gallery page translations"
      contains: "title"
    - path: "src/locales/ar/gallery.json"
      provides: "Arabic gallery page translations"
      contains: "title"
  key_links:
    - from: "src/data/gallery-images.ts"
      to: "public/images/gallery/"
      via: "src path references"
      pattern: "/images/gallery/"
---

<objective>
Create the gallery image data structure and install dependencies for the masonry gallery.

Purpose: Establishes the foundation for the gallery page - image metadata with dimensions (critical for preventing layout shift), accessibility alt text, and bilingual translations.

Output: Gallery dependencies installed, image metadata file with 26 entries, English/Arabic translations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-gallery-page/06-CONTEXT.md
@.planning/phases/06-gallery-page/06-RESEARCH.md

@src/data/services.ts (pattern reference for data files)
@src/locales/en/hse.json (pattern reference for translations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install gallery dependencies</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
Install the two required libraries for the gallery:
- `pnpm add react-masonry-css` - CSS-based masonry grid (zero dependencies, CSS-only approach)
- `pnpm add yet-another-react-lightbox` - Modern lightbox with plugins (has Zoom plugin built-in)

Do NOT install vite-plugin-image-optimizer - that's a Phase 8 concern (Polish & SEO).

Note: Framer Motion already installed (framer-motion ^12.24.3) for animations.
  </action>
  <verify>
`pnpm list react-masonry-css yet-another-react-lightbox` shows both packages installed.
`pnpm build` completes without errors.
  </verify>
  <done>Both gallery dependencies installed and build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create gallery image metadata</name>
  <files>src/data/gallery-images.ts</files>
  <action>
Create a typed gallery images data file following the services.ts pattern:

```typescript
export interface GalleryImage {
  id: number
  src: string
  width: number
  height: number
  alt: string
}
```

Create 26 image entries (jerash-site-01 through jerash-site-26):
- Use `/images/gallery/jerash-site-{NN}.jpg` paths (2-digit zero-padded)
- Assign varied aspect ratios for visual masonry flow:
  - Landscape (16:10): 1600x1000 - for wide field shots
  - Portrait (3:4): 900x1200 - for tall equipment shots
  - Square-ish (4:3): 1200x900 - for balanced compositions
- Mix ratios for visual balance in masonry grid (roughly 10 landscape, 8 portrait, 8 square-ish)
- Alt text should describe oil field operations: workers, equipment, safety gear, drilling, pipelines, etc.
- Order images for visual masonry balance (alternate orientations, spread similar colors)

Export the array and type:
```typescript
export const galleryImages: GalleryImage[] = [...]
```

Note: Actual image files will be placeholder 1px JPEGs for now (same pattern as HSE images). Real images replace later.
  </action>
  <verify>
File exists at src/data/gallery-images.ts.
`grep -c "id:" src/data/gallery-images.ts` returns 26.
TypeScript compiles without errors.
  </verify>
  <done>GalleryImage type exported, galleryImages array has 26 entries with varied dimensions and descriptive alt text.</done>
</task>

<task type="auto">
  <name>Task 3: Add gallery translations</name>
  <files>src/locales/en/gallery.json, src/locales/ar/gallery.json</files>
  <action>
Create gallery page translations following existing patterns (hse.json structure):

**English (src/locales/en/gallery.json):**
```json
{
  "title": "Gallery",
  "description": "Field operations and equipment from our projects across Iraq",
  "seo": {
    "title": "Gallery | Jerash For Oil Field Services",
    "description": "Browse photos from Jerash oil field operations, equipment, and team activities across Iraq."
  },
  "imageOf": "Image {{current}} of {{total}}",
  "loading": "Loading images..."
}
```

**Arabic (src/locales/ar/gallery.json):**
```json
{
  "title": "المعرض",
  "description": "عمليات ميدانية ومعدات من مشاريعنا في أنحاء العراق",
  "seo": {
    "title": "المعرض | شركة جرش لخدمات حقول النفط",
    "description": "تصفح صور من عمليات جرش الميدانية والمعدات وأنشطة الفريق في العراق"
  },
  "imageOf": "صورة {{current}} من {{total}}",
  "loading": "جاري تحميل الصور..."
}
```

Keep translations minimal - the gallery is image-focused with no captions per CONTEXT.md.
  </action>
  <verify>
Files exist at src/locales/en/gallery.json and src/locales/ar/gallery.json.
Both files are valid JSON (no syntax errors).
Both contain "title", "description", "seo" keys.
  </verify>
  <done>English and Arabic gallery translations created with page title, description, SEO meta, and utility strings.</done>
</task>

</tasks>

<verification>
1. `pnpm list react-masonry-css yet-another-react-lightbox` - both installed
2. `pnpm build` - compiles without errors
3. `grep -c "id:" src/data/gallery-images.ts` - returns 26
4. `cat src/locales/en/gallery.json | jq .title` - returns "Gallery"
5. `cat src/locales/ar/gallery.json | jq .title` - returns "المعرض"
</verification>

<success_criteria>
- Both gallery dependencies (react-masonry-css, yet-another-react-lightbox) installed
- GalleryImage interface exported with id, src, width, height, alt fields
- 26 gallery images defined with varied aspect ratios and descriptive alt text
- English and Arabic translations created for gallery page
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-gallery-page/06-01-SUMMARY.md`
</output>
